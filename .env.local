# Local Environment Configuration (for running alembic on host)

# =============================================================================
# Database Configuration (connects to Docker PostgreSQL from host)
# =============================================================================

# PostgreSQL Connection
# Use 'localhost' when connecting from host to Docker container
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=alguerisme
DATABASE_PASSWORD=alguerisme_dev
DATABASE_NAME=alguerisme_dev
DATABASE_ECHO=true
